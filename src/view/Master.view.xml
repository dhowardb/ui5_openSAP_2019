<mvc:View controllerName="com.myorg.myapp.controller.Master" xmlns="sap.m" xmlns:f="sap.f" xmlns:mvc="sap.ui.core.mvc" xmlns:core="sap.ui.core">
    <Panel>
        <FlexBox alignItems="Center">
            <Avatar src="{avatar>/path}" displaySize="XL" displayShape="Square" showBorder="false" press="onPress" imageFitType="Cover" />

            <VBox class="sapUiSmallMarginBegin sapUiSmallMarginTop">
                <Title text="UI5 Air Cargo" level="H2" />
                <Text text="You name it, we ship it!" />
            </VBox>
        </FlexBox>
    </Panel>
    <f:DynamicPage id="masterPage" stickySubheaderProvider="iconTabBar" class="sapUiNoContentPadding">
        <!-- DynamicPage Title -->
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title text="{i18n>appTitle}" level="H2" textAlign="Center" class="sapUiTinyMarginBegin" />
                </f:heading>
            </f:DynamicPageTitle>
        </f:title>
        <f:content>
            <Table
                id="productsTable"
                inset="false"
                items="{
						path: '/Products',
                        events: { dataReceived : '.dataReceived' },
                        parameters: {
                            expand: 'Category,Supplier'
                        },
						sorter: {
							path: 'ID'
						}
					}"
                class="sapFDynamicPageAlignContent"
                width="auto"
                busyIndicatorDelay="0"
            >
                <headerToolbar>
                    <OverflowToolbar>
                        <ToolbarSpacer />
                        <SearchField search="onSearch" width="17.5rem" />
                        <OverflowToolbarButton icon="sap-icon://sort" type="Transparent" press="onSort" />
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column width="12em" demandPopin="true" minScreenWidth="Phone">
                        <Text id="productsTotal" text="Products {$count}" />
                    </Column>
                    <Column width="12em" demandPopin="false" minScreenWidth="Tablet">
                        <Text text="{i18n>masterDescription}" />
                    </Column>
                    <Column width="12em" demandPopin="false" minScreenWidth="Tablet">
                        <Text text="{i18n>categoryInfo}" />
                    </Column>
                    <Column hAlign="End" demandPopin="true" minScreenWidth="Phone">
                        <Text text="{i18n>masterPrice}" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem type="Navigation" press=".onListItemPress">
                        <cells>
                            <ObjectIdentifier title="{Name}" text="{ID}" />
                            <ObjectIdentifier text="{Description}" />
                            <core:Fragment fragmentName='com.myorg.myapp.Fragments.CategoryInfo' type="XML" />
                            <!-- <ObjectListItem type="Inactive">
                                <ObjectAttribute title="{i18n>categoryID}" text="{
                                path: 'Category/ID'
                            }" />
                                <ObjectAttribute title="{i18n>categoryName}" text="{Category/Name}" />
                                <markers>
                                    <ObjectMarker type="{
                                        path: 'Category/Name',
                                        formatter: '.formatter.formatMarkers'
                                    }" />
                                </markers>
                            </ObjectListItem> -->
                            <ObjectNumber number="{Price}" unit="{
                                path: 'ID',
                                formatter: '.formatter.formatCurrency'
                             }" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </f:content>
    </f:DynamicPage>
</mvc:View>
