<mvc:View xmlns:unified="sap.ui.unified" xmlns:core="sap.ui.core" xmlns:form="sap.ui.layout.form" xmlns:f="sap.f" controllerName="com.myorg.myapp.controller.Main" displayBlock="true" xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc">
    <Page title="{i18n>appTitle}" titleAlignment="Center">
        <content>
            <Image src="./images/MoviesHeader.png" width="100%" press=".onPressImage(${i18n>imageLinkMessage})" visible="{=!${device>/system/phone}}" />

            <form:SimpleForm id="form" editable="true" layout="ColumnLayout" title="Find Movies" columnsM="2" columnsL="3" columnsXL="3">
                <form:content>
                    <Label text="City" labelFor="city" />
                    <SearchField id="city" showSearchButton="true" width="100%" placeholder="{i18n>cityPlaceholder}" />
                    <Label text="Genre" labelFor="genre" />
                    <Select id="genre" width="100%">
                        <core:ListItem icon="sap-icon://video" key="" text="{i18n>genreAll}" />
                        <core:ListItem icon="sap-icon://physical-activity" key="Action" text="{i18n>genreAction}" />
                        <core:ListItem icon="sap-icon://electrocardiogram" key="Horror" text="{i18n>genreHorror}" />
                        <core:ListItem icon="sap-icon://paper-plane" key="ScienceFiction" text="{i18n>genreScienceFiction}}" />
                    </Select>
                    <Label />
                    <Button text="{i18n>buttonMovieSearch}" type="Emphasized" class="sapUiSmallMarginTop" press=".onFindMovie('for movies!')" />
                </form:content>
            </form:SimpleForm>
            <PlanningCalendar id="calendar" startDate="{path: 'movies>/initDate', 
										  formatter: '.formatter.formatDate'}" rows="{movies>/movies}" appointmentsVisualization="Filled">
                <toolbarContent>
                    <Title text="{i18n>calendarTitle}" titleStyle="H4" />
                </toolbarContent>
                <rows>
                    <PlanningCalendarRow title="{movies>name}" text="{movies>genre}" appointments="{path: 'movies>appointments', templateShareable: 'true'}">
                        <appointments>
                            <unified:CalendarAppointment startDate="{path: 'movies>startDate' , formatter: '.formatter.formatDate'}" endDate="{path: 'movies>endDate' , formatter: '.formatter.formatDate'}" title="{movies>info}" text="{movies>cinemaName}" icon="{movies>icon}" type="{movies>type}" />
                        </appointments>
                    </PlanningCalendarRow>
                </rows>
            </PlanningCalendar>
        </content>
        <footer>
            <OverflowToolbar class="sapUiMediumMarginTop">
                <content>
                    <ToolbarSpacer />
                    <Link emphasized="true" text="{i18n>footerLink}" href="https://www.imdb.com/" />
                    <ToolbarSpacer />
                </content>
            </OverflowToolbar>
        </footer>
    </Page>

    <!-- <MessagePage
		title="{i18n>appTitle}"
		text="{i18n>appTitle}"
		icon="sap-icon://accept"
		id="page"
		description="{i18n>appDescription}">
		<buttons>
			<Button
				text="{i18n>btnText}"
				press="sayHello" />
		</buttons>
	</MessagePage> -->
</mvc:View>
